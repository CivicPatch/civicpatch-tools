name: 0. Run shared Tests
on:
  pull_request:
    branches:
      - main
    paths:
      - "shared/**"
permissions:
  contents: read

jobs:
  run-shared-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Install dependencies
        run: uv sync --all-packages

      - name: Run shared unit tests
        run: |
          uv run pytest shared/tests
