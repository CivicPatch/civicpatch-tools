services:
  civicpatch:
    extends:
      file: civicpatch/docker-compose.development.yml
      service: civicpatch
    networks:
      - civicpatch_network
  crudder:
    extends:
      file: crudder/docker-compose.yml
      service: crudder
    networks:
      - civicpatch_network

networks:
  civicpatch_network:
    driver: bridge
