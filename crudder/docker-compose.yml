services:
  crudder:
    env_file: .env
    build:
      context: ..
      dockerfile: crudder/Dockerfile
      target: development
    restart: unless-stopped
    ports:
      - "8001:8001"
    volumes:
      - .:/app