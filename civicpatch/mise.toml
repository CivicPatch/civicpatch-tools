[tools]
# python = "3.11"
# poetry = "latest"
#[tasks.setup]
#run = "poetry install && poetry run post-install"
#[tasks.unit]
#run = "PYTHONPATH=./src poetry run pytest -s tests/unit"
#[tasks.integration]
#run = "PYTHONPATH=./src poetry run pytest -s tests/integration"

[tasks.unit]
run = "docker compose run --rm civicpatch xvfb-run poetry run pytest -s tests/unit"
[tasks.integration]
run = "docker compose run --rm civicpatch xvfb-run poetry run pytest -s tests/integration"