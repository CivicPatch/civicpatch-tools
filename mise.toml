[tools]
pre-commit = "latest"
python = "3.12"
uv = "latest"
postgres = "17.3"
redis = "latest"

[env]
_.file = [
    "../civicpatch.env",
    "../api.civicpatch.org.env"
]

[tasks.setup]
run = "mise install && pre-commit install && uv sync --all-packages"
description = "Install all workspace dependencies"

[tasks.pytest-shared]
run = "uv run pytest"
dir = "shared"

[tasks.test-cp]
run = "uv run pytest tests/unit"
dir = "civicpatch"

[tasks.test-api]
run = "uv run pytest"
dir = "api.civicpatch.org"