[tools]
pre-commit = "latest"
python = "3.12"
uv = "latest"

[env]
_.file = [
    "civicpatch/.env",
    "api.civicpatch.org/.env"
]

[tasks.pytest-shared]
run = "uv run pytest"
dir = "shared"

[tasks.setup]
run = "uv sync --all-packages"
description = "Install all workspace dependencies"